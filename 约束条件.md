


# OpenClaw Agent 约束条件 - CPR 项目 nRF24L01 无线通讯完善

你现在是资深嵌入式工程师，精通 STM32F103 + RT-Thread + nRF24L01。
当前项目为 **CPR（心肺复苏）训练/考核/竞赛系统**，包含三个工程：
- cpr-display-main-board_ver1.0 （主显示板，STM32F103ZE，主控面板）
- cpr-sensor-board_ver1.0 （传感器板，STM32F103，人体模型端）
- cpr-remote-device_ver1.0 （遥控设备，STM32F103，手持终端）

**核心任务**：严格基于现有外设资源，完善三板之间的 nRF24L01 无线通讯逻辑（包括连接建立、数据协议、收发流程、ACK 机制、模式切换、有线/无线兼容等），不得破坏现有线程架构和外设初始化。

### 必须严格遵守的硬约束：

1. **硬件资源与引脚（绝对不允许修改）**：
   - **主显示板**：nRF24L01 使用 SPI2 (PB13 SCK, PB14 MISO, PB15 MOSI)，CSN=PB12, CE=PD8, IRQ=PD9。
   - **传感器板**：nRF24L01 使用 SPI3 (PB3 SCK, PB4 MISO, PB5 MOSI)，CSN=PB6, CE=PB7, IRQ=PD2。
   - **遥控设备**：nRF24L01 使用 SPI3 (PB3 SCK, PB4 MISO, PB5 MOSI)，CSN=PA15, CE=PB11, IRQ=PB10（支持 EXTI）。
   - 所有 nRF24L01 相关 GPIO 必须使用 RT-Thread `rt_pin_mode()` / `rt_pin_write()` / `rt_pin_attach_irq()`，不得直接操作寄存器。

2. **通讯角色与模式**：
   - 主显示板：默认作为 **PRX**（接收模式），监听传感器板和遥控设备的连接请求。
   - 传感器板 & 遥控设备：默认作为 **PTX**（发送模式），主动发起连接。
   - 传感器板与遥控设备之间不能直接通信，必须通过主显示板路由。
   - 支持 **PTX/PRX 双模动态切换**（连接建立后传感器板主要 PTX 定时发送数据，主显示板 PRX 接收；主显示板需要下发指令时短暂切换为 PTX）。
   - 支持有线/无线模式切换：主显示板通过 PC6（WIRED_CONNECT）检测，有线模式下必须挂起 nRF24L01_Thread_entry 线程。

3. **现有线程架构（必须保留并在其基础上完善）**：
   - 主显示板：nRF24L01_Thread_entry（优先级9）、nRF24L01_Decode_entry（8）、nRF24L01_Data_Transmit_Thread_entry（10）
   - 传感器板：nRF24L01_Thread_entry（优先级9）
   - 遥控设备：nRF24L01_Thread_entry（9）、nRF24L01_Decode_Thread_entry（9）
   - 通讯相关逻辑放入上述线程中，必要时可以新建线程。

4. **通讯协议要求（必须使用 struct 定义）**：
   - 所有数据包统一使用 typedef struct {} Packet_t; 定义（包含 cmd, seq, payload, crc 等字段）。
   - 支持 **ACK 带载荷** 双向通信。
   - 关键命令类型（至少包含）：
     - ASK_Connect_Control_Panel（连接请求）
     - ACK_Connect（连接应答）
     - SENSOR_DATA（传感器板 → 主显示板：按压深度、吹气量、角度、霍尔状态等）
     - REMOTE_CMD（遥控设备 → 主显示板：模式切换、参数设置、启动/停止等）
     - DISPLAY_FEEDBACK（主显示板 → 遥控设备：人体 LED 状态、成绩数据）
     - HEARTBEAT（心跳保活）
   - 必须实现序列号（seq）防重、简单 CRC8/CRC16 校验、超时重发机制。
   - Payload 长度根据实际数据动态调整（支持动态 Payload）。

5. **nRF24L01 初始化与驱动要求**：
   - 使用 RT-Thread 风格的 nRF24L01 驱动（推荐封装为 nrf24_rtthread.c/.h）。
   - 必须实现：nrf24_init()、nrf24_set_mode_tx()/rx()、nrf24_send_packet()、nrf24_receive_packet()、nrf24_set_channel()、nrf24_set_address() 等接口。
   - 启用自动 CRC、自动重发（最多 15 次）、自动 ACK。
   - IRQ 中断使用 RT-Thread pin 外部中断（rt_pin_attach_irq），优先级合理。
   - 通道（Pipe）和地址必须统一配置（建议使用 5 字节地址，Pipe0 用于 ACK，Pipe1 用于数据）。

6. **业务通讯流程（必须实现）**：
   - **连接建立**：传感器板/遥控设备上电后周期性发送 ASK_Connect_Control_Panel，主显示板收到后回复 ACK_Connect 并记录设备 ID。
   - **数据上报**：传感器板定时（推荐 50~100ms）发送 SENSOR_DATA，主显示板解析后更新数码管、LED 灯条、语音等。
   - **指令下发**：遥控设备发送 REMOTE_CMD 后，主显示板切换为 PTX 短暂发送反馈或控制指令。
   - **有线模式兼容**：有线模式下走 RS485 (USART3)，无线模式走 nRF24L01，代码中用宏或标志位统一抽象通讯层（comm_send() / comm_receive()）。
   - **掉线检测**：连续 5 次心跳失败视为掉线，触发语音提醒和显示更新。

7. **代码规范与兼容性**：
   - 所有代码必须兼容现有 RT-Thread 项目结构（applications/、drivers/、board.c 等）。
   - 使用 rt_kprintf() 或 ulog 进行调试输出。
   - 不得修改 CubeMX 生成的引脚初始化（main.h、board.c 中的外设配置必须保留）。
   - 必须提供清晰的协议定义头文件（cpr_protocol.h）和通讯抽象层（cpr_comm.h）。
   - 生成的代码必须能直接放入对应工程的 applications/ 目录下编译通过（无警告）。

8. **输出要求**：
   - 先输出对本约束的确认（已理解所有硬件引脚、线程、协议要求）。
   - 然后分模块输出代码：
     1. 统一协议头文件（cpr_protocol.h）
     2. nRF24L01 驱动适配层（nrf24_rtthread.c/.h）
     3. 各板通讯线程完善后的代码（重点修改 nRF24L01_Thread_entry 等）
     4. 通讯抽象层（有线/无线统一接口）
     5. 测试用例与调试命令（MSH 命令）
   - 最后给出集成步骤、常见问题排查清单（IRQ 不触发、通讯丢包、地址冲突等）。

如果你对任何硬件资源、引脚或现有线程有疑问，必须先提问确认，不得擅自假设或修改。





现在，请根据以上所有约束，开始完善 CPR 项目三板 nRF24L01 无线通讯逻辑。